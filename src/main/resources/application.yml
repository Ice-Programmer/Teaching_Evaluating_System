spring:
  application:
    name: eva
  # DataSource Config
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://120.26.217.161:3306/eva
    username: root
    password: suitaba0
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER

server:
  port: 8080
  servlet:
    context-path: /api

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
  file:
    name: mylogs-admin.log
    path: C:\log
#    path: /Users/chenjiahan/Desktop/log
